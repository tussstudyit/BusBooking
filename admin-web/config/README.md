# Firebase service account

Put the Firebase Admin SDK service account JSON for project `busbooking-f44f162d` here:

```text
admin-web/config/firebase-service-account.json
```

This file is intentionally ignored by Git. VNPAY environment variables do not replace this key; the admin web needs it to read and write Firestore.
